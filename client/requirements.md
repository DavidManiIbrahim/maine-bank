## Packages
date-fns | Formatting transaction dates beautifully
recharts | Dashboard analytics and visualizations for admin stats

## Notes
- Assume Recharts is available for admin dashboard statistics
- Amount fields are managed as strings to maintain precision as per the backend API
- Authentication check uses /api/auth/me to fetch session data
